def SuperPrime(x:int) -> bool:
    list = []
    j = 1
    for h in range(len(str(x))):
        j+=1
        str(x)[:-1].append(list)
        for k in list:
            for m in range(len(list)):
                if int(m)%j == 0:
                    
